# Handy FSM

A finite state machine solution for Unity.

This package is deprecated.

HandyFSM is no longer maintained as a supported standalone package.
The repository remains available as the project source for the module, including
legacy and project-specific samples that may depend on external packages.
